language: python
python:
  - "2.7"
  - "3.4"
  - "3.5"
  - "3.6"
virtualenv:
    system_site_packages: true
branches:
  only:
    - master
    - /\d+\.\d+\..+/
before_install:
  - SITE_PKG_DIR=$VIRTUAL_ENV/lib/python$TRAVIS_PYTHON_VERSION/site-packages
  - rm -f $VIRTUAL_ENV/lib/python$TRAVIS_PYTHON_VERSION/no-global-site-packages.txt
install:
  - sudo apt-get update -qq
  - sudo apt-get install python-numpy python-matplotlib -qq
  - sudo pip install scipy lxml bs4 coverage
script: "nosetests -sv ."
notifications:
  recipients:
    - alexander.fabisch@dfki.de
  email:
    on_success: change
    on_failure: always
