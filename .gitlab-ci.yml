before_script:
  - virtualenv pytransform_env
  - source pytransform_env/bin/activate
  - pip install numpy nose sphinx matplotlib

stages:
  - test

nosetests:
  stage: test
  script:
    - nosetests -sv --with-coverage

sphinxdoc:
  stage: test
  script:
    - cd doc; make html
